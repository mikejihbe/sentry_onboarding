.. highlight:: shell

============
Installation
============


Stable release
--------------

To install sentry-onboarding, run this command in your terminal:

.. code-block:: console

    $ pip install sentry_onboarding

This is the preferred method to install sentry-onboarding, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for sentry-onboarding can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/mikejihbe/sentry_onboarding

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/mikejihbe/sentry_onboarding/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/mikejihbe/sentry_onboarding
.. _tarball: https://github.com/mikejihbe/sentry_onboarding/tarball/master
